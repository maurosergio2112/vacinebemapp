// autolinking.gradle
def setupAutolinking(project) {
    // Configuração padrão para autolinking no React Native
    project.ext.react = [
        enableHermes: false
    ]
}

setupAutolinking(project)
